#include<stdio.h>
int main()
{
    int i,n,j=o,k=0;
    int u[10],t[10];
    scanf("%d",&n);
        for(i=0;i<n;i++){
            scanf("%d %d",&u[j],&t[k])
            j++,k++;
            s=u[j]*t+0.5*a*t[k]*t[k];
            printf("%d",&s);
    }
}
