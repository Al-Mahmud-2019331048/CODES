#include<bits/stdc++.h>

using namespace std;

int main(){
        ios::sync_with_stdio(0);cin.tie(nullptr);
    //mahmud48
        int cnt=0;
    string s,s1="hello";
    cin>>s;
    while(1){
        cout<<"fvebrhcbfhfbhv"<<endl;
    }
    for (int i = 0; i < s.size(); ++i)
    {
        if(s[i]==s1[cnt])
            cnt++;
    }
    if(cnt==5)
        cout<<"YES"<<endl;
    else
        cout<<"NO"<<endl;
    return 0;
}
