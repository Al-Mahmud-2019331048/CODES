#include<bits/stdc++.h>

using namespace std;

//mahmud48

int main()
{
    int n,m;
    cin>>n>>m;
    vector<string>s(m);
    for(int i=0;i<n;i++)
        cin>>s[i];
    vector<int>v(m);
    for (int i = 0; i < m; ++i)
        cin>>v[i];
    for (int i = 0; i < m; ++i)
    {
        /* code */
    }


    return 0;
}
