#include<bits/stdc++.h>
#include<cmath>
using namespace std;

int main()
{
    //mahmud48
    double m,n,a;
    long long int x;
    cin>>m>>n>>a;
    x=ceil(n / a) * ceil(m / a);
    cout<<x<<endl;

    return 0;
}
